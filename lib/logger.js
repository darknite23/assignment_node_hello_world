//log method that takes two parameters.
        //first parameter should be the message to log
        //second parameter should be a string specifiying the logging level. 


//info method that only takes a single parameter for the message and ouputs it at the info level. 
    //colorize the output accordinlgy

//warning method that only takes a single parameter for "                                        "

//erro.....

/*
var log = function(message,str){
    console.log(message);//the 
    //console.log(str);//should be a trcky logging day. 
    console.log(str);

},
*/

/*

var logMe = require('./log');

logMe("Message logged at","INFO");

var info = require('./info');

info("INFO");

var warning = require('./warning');

warning("WARNING");

var error = require('./error');

error("ERROR");
*/